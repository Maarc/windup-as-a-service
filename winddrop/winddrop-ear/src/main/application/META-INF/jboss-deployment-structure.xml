<jboss-deployment-structure>
	<ear-subdeployments-isolated>false</ear-subdeployments-isolated>
	<!-- This corresponds to the top level deployment. For a war this is the war's module, for an ear -->
	<!-- This is the top level ear module, which contains all the classes in  the EAR's lib folder -->
	<deployment>
		<!-- Exclusions allow you to prevent the server from automatically adding some dependencies -->
		<exclusions>
		</exclusions>
		<!-- This allows you to define additional dependencies, it is the same as using the Dependencies: manifest attribute -->
		<dependencies>
		</dependencies>
		<!-- These add additional classes to the module. In this case it is the same as including the jar in the EAR's lib directory -->
		<resources>
		</resources>
	</deployment>
	<sub-deployment name="winddrop-windup-legacy-ejb.jar">
		<exclusions>
			<module name="org.apache.log4j" />
		</exclusions>
		<dependencies>
			<module name="org.jboss.windup" export="true" meta-inf="export" />
		</dependencies>
	</sub-deployment>
	<sub-deployment name="winddrop-windup-ejb.jar">
        <exclude-subsystems>
        </exclude-subsystems>
        <exclusions>
        </exclusions>
		<dependencies>
			<module name="org.jboss.windup" slot="2.1.0.Final" export="true" meta-inf="import" />
		</dependencies>
	</sub-deployment>
	<sub-deployment name="winddrop-war.war">
		<dependencies>
		</dependencies>
	</sub-deployment>
</jboss-deployment-structure>